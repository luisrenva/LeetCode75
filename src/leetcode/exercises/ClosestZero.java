package leetcode.exercises;

//find 2 elements of an array whose sum is closest to 0 find 2 elements of an array whose sum is closest to 5
class ClosestZero {

    public static void main (String ... args) {
        // int values [] = {1, 60, -10, 70, -80, 85};
        // calculateClosestToZero(values);
        String  test = "javadeveloper";
        String [] array = test.split("");
        for (String value: array) {
            System.out.println(value);
        }
        System.out.println(array.length);
    }


    // public void calculateClosestToZero(int array []) {
    //     for (int i=0; i < array.length; i++) {
    //         for (int j=0 j < )
    //     }
    // }

}